/**
 * One delivery-artifact ceiling for provider reads, persisted verification,
 * receipts, gallery eligibility, sharing, and authenticated downloads.
 */
export const MAX_DELIVERY_ARTIFACT_BYTES = 32 * 1024 * 1024;
